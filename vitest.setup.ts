import '@testing-library/jest-dom'

// import '@testing-library/jest-dom/extend-expect'

// import '@testing-library/jest-dom/dist/extend-expect'
// import * as matchers from '@testing-library/jest-dom/matchers'
// import * as matchers2 from '@testing-library/jest-dom/dist/matchers'
// console.log('before: ', expect)
// import 'vitest/globals'
// import { describe, expect, test, vitest } from 'vitest'
// export * from 'vitest'

// for (const key in tester) {
//   if (tester.hasOwnProperty(key)) {
//     // const element = tester[key]
//     console.log(`${key}`)
//   }
// }
// for (const [key, value] of Object.keys(expect(2).toBe(2)) {
//   console.log(`Key: ${key} | Value: ${value}`)
// })

// console.log('expect.extend: ', expect.extend.toString())
// console.log('expect.extend(): ', expect.extend(matchers2))
// expect.extend(matchers2)
//
// console.log('after: ', expect(2))
// // console.log('matchers: ', matchers)
// export default expect
